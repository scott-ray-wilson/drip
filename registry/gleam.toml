name = "drip_registry"
description = "The Drip element catalog: registry data types and lookup helpers."
version = "0.1.0"
licences = ["MIT"]

# Internal package: never published to Hex (see ../PUBLISHING.md).

[dependencies]
gleam_stdlib = ">= 1.0.0 and < 2.0.0"

[dev_dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
